package masyvai;

import java.util.Arrays;
import java.util.List;

import static jdk.nashorn.internal.objects.NativeMath.random;

// lifto uzduotis.
public class ArrayTestField {
    public static void main(String[] args) {
        /*
        int[] arrayOfInts = {10, 15, 20, 25, 30};
        System.out.println(arrayOfInts[0]); // prints 10
        System.out.println(arrayOfInts[2]); // prints 20
        System.out.println(arrayOfInts[4]); // prints 30
        // Išspausdins 10 15 20 25 30
        for (int i = 0; i < arrayOfInts.length; i++) {
            System.out.print(arrayOfInts[i] + " ");
        }
        System.out.println(); // Spausdinama tuščia eilutė
        // Išspausdins 10 15 20 25 30
        for(int i : arrayOfInts) {
            System.out.print(i + " ");
        }
        System.out.println();
        List<Integer> integerList = Arrays.asList(10, 15, 20, 25, 30);
        integerList.forEach(number -> System.out.print(number + " "));



        String[][] arrayOfNames ={ {"Jonas", "Petras", "Antanas"}};
        for (int i = 0; i <arrayOfNames.length; i++){
            System.out.println(arrayOfNames[i]+ arrayOfNames[i][i+1] + " ");
        }


        int height = 200;
        int width = 200;

        int[][] location = new int [height][width];

        for (int i = 0; i < width; i++) {
            for (int j = 0; j < height; j++) {
                location[i][j] = (int)(Math.random());
            }
        }
        for (int i = 0; i < width; i++) {
            for (int j = 0; j < height; j++) {
                double[][] location;
                stroke(location[i][j]);
                point(i,j);
            }
        }
    }

    private static void point(int i, int j) {
    }
    */

    }
}
